# LlamaRec `geometry_prune` Stage 3 剪枝遗忘优化提示词

下面内容是一份可以直接交给 AI 修改实验代码的提示词。目标是优化 `geometry_prune` 方法的 Stage 3 搜索效率，同时保持方法性质为**剪枝遗忘**，不引入梯度遗忘、微调、蒸馏或重新训练。

---

## 任务背景

你需要优化 LlamaRec 项目中 `geometry_prune` 方法的 Stage 3。

项目根目录：

```bash
/home/hjy/projects/LlamaRec-main
```

统一运行入口预计为：

```bash
/home/hjy/projects/LlamaRec-main/run_unlearning.py
```

方法实现预计位于：

```bash
projects/LlamaRec-main/scripts/smoke_retain_prior_cf_lora_prune.py
```

如果实际文件位置不同，请先根据 `METHOD_REGISTRY`、方法注册表或 `geometry_prune` 关键字搜索真实实现位置，不要凭空重写。

---

## 绝对优先要求：修改前必须备份上一个实验代码

在任何代码修改前，必须完成备份。备份失败时必须立即停止，不允许继续修改。

请在项目根目录执行类似流程：

```bash
cd /home/hjy/projects/LlamaRec-main

timestamp=$(date +"%Y%m%d_%H%M%S")
backup_dir="backups/stage3_prune_backup_${timestamp}"
mkdir -p "${backup_dir}"

git status --short > "${backup_dir}/git_status_before.txt"
git rev-parse HEAD > "${backup_dir}/git_commit_before.txt"
git diff > "${backup_dir}/git_diff_before.patch"

cp -a run_unlearning.py "${backup_dir}/run_unlearning.py"
cp -a unlearning "${backup_dir}/unlearning"
```

如果仓库允许，也建议同时创建备份分支：

```bash
git branch "backup/stage3-prune-before-${timestamp}"
```

备份要求：

- 必须保存修改前的 `git status`；
- 必须保存修改前的 `git diff`；
- 必须保存当前 commit hash；
- 必须备份 `run_unlearning.py` 和 `unlearning/` 目录；
- 不允许使用 `git reset --hard`、强制覆盖、删除原代码等破坏性操作；
- 修改结束后，在最终回复中明确写出备份路径。

---

## 方法边界：必须是剪枝遗忘，不是梯度遗忘

本次修改必须保持为**剪枝遗忘**。

禁止行为：

- 禁止使用梯度上升做遗忘；
- 禁止微调；
- 禁止蒸馏；
- 禁止完整重训练；
- 禁止 `optimizer.step()`；
- 禁止通过更新模型权重实现遗忘；
- 禁止把 Stage 3 改成训练过程；
- 禁止用一阶 Taylor 梯度代理作为正式筛选依据。

允许行为：

- 临时 mask 某个 LoRA rank；
- 评估 mask 前后的 residual/protection 指标变化；
- 根据指标变化选择要永久剪枝的 LoRA rank；
- 最终保存 selected mask 或剪枝后的 LoRA rank 状态。

核心原则：

```text
只通过 LoRA rank mask/prune 完成遗忘。
不反向传播。
不计算梯度。
不更新参数。
```

如果现有代码里 Stage 3 使用了梯度、Taylor score、`loss.backward()` 或 optimizer 逻辑，需要删除或绕开这些逻辑，改成无梯度 mask 消融式剪枝筛选。

---

## 原 Stage 3 问题判断

当前 Stage 3 的主要性能瓶颈不是 GPU 不够，而是搜索复杂度设计不合理。

原流程大致是：

```text
对每个 LoRA rank
    在完整 residual target 和完整 protection target 上做 mask 消融评估
    贪心选择一个 rank
    重新评估剩余 rank
重复直到选够目标剪枝数量
```

设：

```text
R = 512                         # LoRA rank 总数
K = 25                          # 目标剪枝 rank 数
F = 2048                        # residual target 数
P = 40959                       # protection target 数
T_full = F + P = 43007
```

原始逐 rank 全量贪心评估次数约为：

```text
(512 + 511 + ... + 488) * 43007
= 12500 * 43007
= 537,587,500 次 rank-target 精确评估
```

这就是 Stage 3 慢的根本原因。

---

## 新 Stage 3 总体方案

将 Stage 3 改成严格剪枝遗忘的 coarse-to-fine 流程：

```text
Stage 3A: 构建小规模校准集
Stage 3B: 无梯度 LoRA rank 剪枝敏感度粗筛
Stage 3C: 生成 shortlist 与 reserve pool
Stage 3D: 分块式精确 mask 消融剪枝
Stage 3E: 完整 residual/protection 最终验证
```

对应流程：

```text
全部 LoRA rank
    ↓
构建 residual/protection 校准子集
    ↓
对全部 rank 做无梯度临时 mask 粗筛
    ↓
生成 shortlist，例如 64 个 rank
    ↓
按 10 + 10 + 5 分块剪枝
    ↓
每轮只在剩余 shortlist 上重评估
    ↓
最终完整数据验证一次
    ↓
保存 pruning_summary.json 和中间结果
```

---

## Stage 3A：构建校准子集

完整 target 只能用于最终验证，不能在每个 rank 的筛选阶段反复遍历。

默认参数：

```text
forget_calibration_size = 128
protection_calibration_size = 512
C = 128 + 512 = 640
```

遗忘校准集 `C_f` 从 Stage 1 的 residual target 中选择：

- 50%：residual weight 或 residual strength 最高的 target；
- 30%：Top-K 边界附近或 boundary sensitivity 高的 target；
- 20%：按 user/item/interaction 分层随机补充，固定随机种子。

保护校准集 `C_p` 从 Stage 2 的 protection target 中选择：

- 优先 protection weight 高的样本；
- 优先与待遗忘交互语义或协同关系接近的样本；
- 优先 Top-K 边界附近的保护样本；
- 按 user_id、item_id 或 target 类型分桶，保证覆盖度；
- 不允许直接普通随机取 512 条。

保存：

```text
calibration_targets.json
```

至少包含：

```json
{
  "forget_total": 2048,
  "forget_selected": 128,
  "protection_total": 40959,
  "protection_selected": 512,
  "forget_sampling_composition": {
    "high_residual": 64,
    "boundary_sensitive": 38,
    "stratified_random": 26
  },
  "seed": 42
}
```

---

## Stage 3B：无梯度 LoRA rank 剪枝敏感度粗筛

这一阶段必须是无梯度剪枝评估，不允许 Taylor 梯度代理。

对每个 LoRA rank `q`：

1. 保存当前 mask 状态；
2. 临时 mask：

```text
lora_A[q, :]
lora_B[:, q]
```

3. 在校准集 `C_f ∪ C_p` 上评估 mask 前后指标变化；
4. 恢复 mask 状态；
5. 记录该 rank 的遗忘收益与保护损伤。

指标方向必须统一：

```text
forget_gain 越大越好
protection_damage 越小越好
```

建议定义：

```text
forget_gain_q =
baseline_forget_metric - masked_forget_metric
```

含义：剪掉 rank `q` 后，遗忘残留下降越多越好。

```text
protection_damage_q =
masked_protection_metric - baseline_protection_metric
```

含义：剪掉 rank `q` 后，保护集损伤越小越好。

如果当前代码中 residual/protection 指标方向不同，必须按现有指标方向调整公式，但最终仍要满足：

```text
forget_gain 越大越好
protection_damage 越小越好
```

重要实现要求：

- 使用 `model.eval()`；
- 使用 `torch.inference_mode()`；
- 不调用 `loss.backward()`；
- 不构建计算图；
- 不执行 `optimizer.step()`；
- 不改变模型权重；
- 每个候选 rank 的临时 mask 必须用 `try/finally` 或上下文管理器恢复。

保存：

```text
rank_prune_probe_scores.json
```

每条至少包含：

```json
{
  "rank_id": "model.layers.12.self_attn.q_proj::rank_3",
  "module_name": "model.layers.12.self_attn.q_proj",
  "rank_index": 3,
  "forget_gain": 0.0,
  "protection_damage": 0.0,
  "forget_gain_normalized": 0.0,
  "protection_damage_normalized": 0.0,
  "passes_protection_constraint": true
}
```

---

## Stage 3C：保护约束下生成 shortlist

不要使用难解释的：

```text
forget_gain - lambda * protection_damage
```

推荐使用保护约束优先：

1. 对 `forget_gain` 和 `protection_damage` 分别做稳健归一化；
2. 用 protection damage 的分位数设定保护阈值；
3. 默认：

```text
protection_risk_quantile = 0.70
rank_shortlist_size = 64
rank_reserve_size = 128
```

选择逻辑：

```text
先过滤 protection_damage 不高的 rank
再按 forget_gain 从高到低排序
取前 64 个作为 active shortlist
取前 128 个作为 reserve pool
```

引入 reserve pool 的原因：

- 第一轮剪掉一批 rank 后，剩余 rank 的边际效果可能变化；
- reserve pool 可以在后续轮次补充候选；
- 避免第一轮粗筛漏掉后续变优的 rank。

保存：

```text
rank_shortlist.json
rank_reserve_pool.json
```

---

## Stage 3D：分块式精确 mask 消融剪枝

不要再单独做一次 Stage 3D 后又在 Stage 3E 重复评估。

精确 mask 消融应该直接放进分块剪枝循环中。

默认 block schedule：

```text
10,10,5
```

如果目标剪枝数不是 25，需要自动调整：

```text
target=5  -> [5]
target=12 -> [10,2]
target=25 -> [10,10,5]
```

每轮流程：

```text
1. 基于当前已永久 mask 的模型，计算本轮 baseline
2. 对剩余 shortlist rank 逐个做临时 mask
3. 在 C_f ∪ C_p 上计算 forget_gain 和 protection_damage
4. 按保护优先策略选择本轮 block 个 rank
5. 对选中的 rank 应用永久 mask
6. 对本轮选中的 block 做一次联合验证
7. 保存本轮 JSON 和 checkpoint
```

注意：

- 不要每剪一个 rank 就重新评估全部候选；
- 每轮只评估剩余 shortlist；
- 已剪 rank 不再进入候选；
- 每轮选完一批后再永久 mask；
- 下一轮 baseline 必须基于当前已剪枝模型重新计算；
- 每轮 block 联合验证可以发现“单个安全、组合不安全”的情况。

默认三轮评估规模：

```text
Round 1: 64 candidates * 640 targets
Round 2: 54 candidates * 640 targets
Round 3: 44 candidates * 640 targets
```

保存：

```text
rank_exact_scores_round1.json
rank_exact_scores_round2.json
rank_exact_scores_round3.json
selected_rank_masks.json
```

每轮 JSON 至少包含：

```json
{
  "round": 1,
  "requested_block_size": 10,
  "actual_selected_count": 10,
  "baseline_forget_metric": 0.0,
  "baseline_protection_metric": 0.0,
  "candidate_count": 64,
  "selected": [],
  "block_joint_validation": {
    "forget_metric": 0.0,
    "protection_metric": 0.0
  },
  "fallback_used": false,
  "elapsed_seconds": 0.0
}
```

---

## Stage 3E：完整数据最终验证

完整 residual target 和完整 protection target 只能在最终 mask 后正式评估一次。

完整集：

```text
F = 2048
P = 40959
T_full = 43007
```

允许：

```text
Stage 3 开始前读取或计算一次完整 baseline
全部分块剪枝结束后，对最终 mask 做一次完整 after 验证
```

禁止：

```text
for every rank:
    evaluate all residual targets
    evaluate all protection targets
```

最终保存：

```text
pruning_summary.json
metrics_unlearning.json
forget_residual_before_after.json
```

`pruning_summary.json` 至少包含：

```json
{
  "status": "completed",
  "stage3_mode": "coarse_to_fine_pruning",
  "forget_method_type": "pruning_unlearning",
  "uses_gradient": false,
  "uses_optimizer_step": false,
  "total_rank_count": 512,
  "target_prune_count": 25,
  "actual_prune_count": 25,
  "rank_shortlist_size": 64,
  "rank_reserve_size": 128,
  "forget_calibration_size": 128,
  "protection_calibration_size": 512,
  "block_schedule": [10, 10, 5],
  "full_forget_target_count": 2048,
  "full_protection_target_count": 40959,
  "selected_rank_ids": [],
  "backup_path": "backups/stage3_prune_backup_YYYYMMDD_HHMMSS"
}
```

---

## 计算复杂度与评估次数

### 原始逐 rank 全量贪心

设：

```text
R = 512
K = 25
T_full = 43007
```

原始评估次数：

```text
(R + R - 1 + ... + R - K + 1) * T_full
= (512 + 511 + ... + 488) * 43007
= 537,587,500 次 rank-target 精确评估
```

复杂度：

```text
O(K * R * T_full)
```

### 新的严格剪枝遗忘方案

默认：

```text
R = 512
C_f = 128
C_p = 512
C = 640
S = 64
block schedule = [10, 10, 5]
T_full = 43007
```

无梯度全 rank 粗筛：

```text
512 * 640 = 327,680
```

分块精确重评：

```text
Round 1: 64 * 640 = 40,960
Round 2: 54 * 640 = 34,560
Round 3: 44 * 640 = 28,160

合计 = 103,680
```

每轮 baseline：

```text
3 * 640 = 1,920
```

每轮 block 联合验证：

```text
3 * 640 = 1,920
```

最终完整 after 验证：

```text
43,007
```

总评估次数约为：

```text
327,680 + 103,680 + 1,920 + 1,920 + 43,007
= 478,207
```

如果完整 before baseline 没有缓存，需要再加：

```text
43,007
```

则总评估次数约为：

```text
521,214
```

因此复杂度从：

```text
O(K * R * T_full)
```

降低为：

```text
O(R * C + rounds * S * C + T_full)
```

默认规模下从约：

```text
5.38 亿次
```

降低到约：

```text
47.8 万到 52.1 万次
```

约减少：

```text
1000 倍级别
```

---

## 新增参数建议

请在不破坏旧命令的前提下新增：

```text
--stage3_search_mode
默认 coarse_to_fine_pruning
可选 coarse_to_fine_pruning, full_greedy

--forget_calibration_size
默认 128

--protection_calibration_size
默认 512

--rank_shortlist_size
默认 64

--rank_reserve_size
默认 128

--protection_risk_quantile
默认 0.70

--prune_block_schedule
默认 10,10,5

--stage3_resume
默认 true

--stage3_force_recompute
默认 false

--stage3_full_eval_frequency
默认 final_only
当前只支持 final_only
```

必须保留旧 Stage 3 作为：

```text
--stage3_search_mode full_greedy
```

默认使用新模式：

```text
--stage3_search_mode coarse_to_fine_pruning
```

---

## mask 安全要求

必须封装统一的 mask 管理函数，不要在多个地方重复写不一致的 mask 操作。

建议实现：

```python
temporary_rank_mask(rank_id)
apply_permanent_rank_masks(rank_ids)
restore_rank_masks(mask_state)
get_current_rank_mask_state()
```

要求：

- 临时 mask 前保存当前状态；
- 临时 mask 后必须恢复；
- 异常时也必须恢复；
- 永久 mask 只在每轮选定 block 后应用；
- 不允许临时 mask 泄漏到下一个候选；
- 不允许重复选择同一个 rank；
- 恢复运行时必须重新应用已选永久 mask。

---

## 断点恢复要求

新增：

```text
--stage3_resume true
--stage3_force_recompute false
```

恢复顺序：

1. 如果 `calibration_targets.json` 存在且配置一致，复用；
2. 如果 `rank_prune_probe_scores.json` 存在且配置一致，复用；
3. 如果 `rank_shortlist.json` 存在且配置一致，复用；
4. 如果某轮 `rank_exact_scores_roundN.json` 和 `selected_rank_masks.json` 存在，恢复永久 mask；
5. 从下一个未完成 round 继续；
6. 如果 `pruning_summary.json` 中 `status=completed`，默认不重复运行；
7. 如果配置 hash 不一致，不允许静默复用旧缓存。

写 JSON 时必须使用原子保存：

```text
先写临时文件，再 rename
```

---

## 日志要求

日志至少包含：

```text
[Stage3][Backup]
backup_path=...

[Stage3][Calibration]
forget selected=128/2048
protection selected=512/40959

[Stage3][Probe]
mode=no_gradient_mask_ablation
total_ranks=512
calibration_targets=640

[Stage3][Shortlist]
shortlist=64
reserve=128
protection_quantile=0.70

[Stage3][Round 1/3]
remaining_candidates=64
selected=10

[Stage3][Round 2/3]
remaining_candidates=54
selected=10

[Stage3][Round 3/3]
remaining_candidates=44
selected=5

[Stage3][FinalValidation]
forget_targets=2048
protection_targets=40959

[Stage3][Completed]
selected=25/512
summary_path=...
```

所有 fallback、异常 rank、缓存命中、配置不一致、mask 恢复都必须写日志。

---

## 修改前代码检查清单

修改前必须先找到并记录：

1. Stage 1、Stage 2、Stage 3 的实际函数位置；
2. LoRA rank 枚举逻辑；
3. rank mask 应用逻辑；
4. residual target 数据结构；
5. protection target 数据结构；
6. 当前每个 rank 的精确评估函数；
7. 模型评分函数，例如 `_score_candidate_list`；
8. 当前输出文件；
9. 当前 resume 机制；
10. `BaseUnlearningMethod.run()` 接口约束；
11. `METHOD_REGISTRY` 注册方式；
12. 当前是否重复执行 prompt 构造、tokenization、baseline logits、Top-K 边界计算。

---

## 测试要求

修改完成后必须执行：

### 1. 语法检查

```bash
python -m py_compile 修改过的 Python 文件
```

### 2. 参数解析测试

验证新增参数能解析，旧命令不加新参数也能运行。

### 3. 小规模 smoke test

建议命令模板：

```bash
python run_unlearning.py \
  --method geometry_prune \
  --stage3_search_mode coarse_to_fine_pruning \
  --forget_calibration_size 32 \
  --protection_calibration_size 64 \
  --rank_shortlist_size 16 \
  --rank_reserve_size 32 \
  --prune_block_schedule 4,4 \
  --stage3_resume true \
  --stage3_force_recompute true \
  --stage3_full_eval_frequency final_only
```

### 4. mask 恢复测试

验证：

- mask 前输出；
- 临时 mask 后输出发生变化；
- restore 后输出恢复；
- 允许浮点误差。

### 5. 无梯度测试

验证 Stage 3：

- 没有 `loss.backward()`；
- 没有 `optimizer.step()`；
- 没有更新模型参数；
- 使用 `torch.inference_mode()`；
- 修改前后模型权重一致，只有 mask 状态变化。

### 6. 分块计划测试

验证：

```text
target=25, schedule=10,10,5 -> [10,10,5]
target=5, schedule=10,10,5  -> [5]
target=12, schedule=10,10,5 -> [10,2]
```

### 7. resume 测试

模拟第一轮结束后中断，再启动时必须从第二轮继续，不能重新执行 Stage 1、Stage 2 和 rank probe。

### 8. legacy 模式测试

```bash
python run_unlearning.py \
  --method geometry_prune \
  --stage3_search_mode full_greedy
```

必须仍能进入旧实现，方便复现实验。

---

## 正式实验参数建议

当前 0.05 剪枝实验建议：

```bash
python run_unlearning.py \
  --method geometry_prune \
  --stage3_search_mode coarse_to_fine_pruning \
  --forget_calibration_size 128 \
  --protection_calibration_size 512 \
  --rank_shortlist_size 64 \
  --rank_reserve_size 128 \
  --protection_risk_quantile 0.70 \
  --prune_block_schedule 10,10,5 \
  --stage3_resume true \
  --stage3_force_recompute false \
  --stage3_full_eval_frequency final_only
```

正式实验如果显存和时间允许，可提高为：

```text
forget_calibration_size = 256
protection_calibration_size = 1024
rank_shortlist_size = 96
rank_reserve_size = 192
```

---

## 验收标准

修改后必须满足：

1. Stage 1 和 Stage 2 方法语义不变；
2. 遗忘方式仍是 LoRA rank 剪枝；
3. 不使用梯度遗忘；
4. 不调用 `loss.backward()`；
5. 不调用 `optimizer.step()`；
6. 不更新模型权重；
7. 默认不再对全部 `512 rank * 40959 protection target` 做全量逐轮评估；
8. 只有校准集参与候选筛选；
9. 只有 shortlist 进入分块精确消融；
10. 剪枝按 block 执行，不是 25 次逐 rank 全量贪心；
11. 完整 target 只在最终 mask 后执行一次正式验证；
12. 旧 `full_greedy` 模式保留；
13. 中断后可以恢复；
14. 所有中间 JSON 和 summary 正常保存；
15. 修改前备份路径写入最终 summary 和最终回复。

---

## 最终回复必须包含

完成修改后，请返回：

1. 修改前备份路径；
2. 实际修改的文件列表；
3. 每个文件的修改内容；
4. 找到的原 Stage 3 性能瓶颈；
5. 新 Stage 3 的完整调用链；
6. 无梯度 rank probe 的代码位置；
7. calibration sampling 的代码位置；
8. shortlist/reserve pool 生成位置；
9. 分块剪枝位置；
10. 最终 full evaluation 位置；
11. resume 机制位置；
12. 新增参数列表；
13. 运行过的测试和结果；
14. 小规模 smoke test 命令；
15. 当前 0.05 正式实验命令；
16. 预期生成的输出文件；
17. 尚未解决的风险或兼容性问题。

不要声称测试通过，除非已经实际执行命令并看到成功结果。
