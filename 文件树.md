# LlamaRec 项目文件树与作用说明

本文档面向实验助手和维护者，说明 `/home/hjy/projects/LlamaRec-main` 的目录结构、主要文件职责和实验产物约定。

项目是 LlamaRec 两阶段推荐系统的 PyTorch 实现，当前已扩展固定交互 split 的 machine unlearning 流程。第一阶段 LRURec 生成候选，第二阶段 Llama/LoRA ranker 对候选重排；`run_unlearning.py` 在固定 forget/retain split 上运行方法、采集 before/after 预测并计算遗忘指标。

说明：仓库总计约 539 个非 Git/非 Python cache 文件，且包含约 29 GB 数据、模型 checkpoint、预测缓存和实验日志。为保持可读性，源码逐文件列出；大规模数据、权重、JSONL 预测缓存按稳定的目录与文件模式说明，而非逐条展开每一份重复产物。

```text
LlamaRec-main/
|
|-- README.md
|   原始 LlamaRec 项目说明：两阶段推荐框架、依赖、LRU retriever 和 LLM ranker 训练方式。
|-- 文件树.md
|   本文件：项目结构和职责索引。
|-- CODEX_THREE_SYMBOL_CHECKS.md
|   对 retain_prior_cf_lora_prune 正式调用链、LoRA mask 与关键设计的核查记录。
|-- .gitignore
|   忽略 data/、experiments/、wandb/、模型权重与 Python cache 等生成内容。
|-- config.py
|   全局 argparse 配置、数据筛选条件、模型/LoRA/训练超参数和实验根目录。
|-- requirements.txt
|-- requirements_pip.txt
|-- requirements_conda_export.txt
|   Python/Conda 依赖描述与环境导出。
|
|-- train_retriever.py
|   第一阶段入口：训练 LRURec，并导出 retrieved.pkl 候选集合。
|-- train_ranker.py
|   第二阶段入口：加载 Llama 基座，配置 LoRA，训练并测试 LLM ranker。
|-- run_unlearning.py
|   统一的交互级 unlearning 入口：解析固定 split、加载 checkpoint、采集 before/after
|   predictions、从 METHOD_REGISTRY 调用方法，并写入评估与配置产物。
|
|-- data/
|   数据集及其预处理缓存；大文件，通常不进入 Git。
|   |-- ml-100k/
|   |   MovieLens 100K 原始/小型数据副本：ratings.csv、movies.csv、tags.csv、links.csv，
|   |   以及 ml-latest-small/ 对应副本和 README。
|   `-- preprocessed/
|       `-- ml-100k_min_rating0-min_uc5-min_sc5*/dataset.pkl
|           按 min_rating/min_uc/min_sc 过滤后的序列推荐数据缓存；`*_unlearn0.2` 为
|           unlearning 实验使用的预处理变体。
|
|-- datasets/
|   数据集定义与预处理逻辑。
|   |-- __init__.py              数据集注册表 DATASETS。
|   |-- base.py                  共享数据加载、split 注入与序列处理基础类。
|   |-- ml_100k.py               MovieLens 100K 适配器。
|   |-- beauty.py                Amazon Beauty 适配器。
|   |-- games.py                 Amazon Games 适配器。
|   `-- utils.py                 数据集通用辅助函数。
|
|-- dataloader/
|   PyTorch Dataset/DataLoader 与 prompt 构造。
|   |-- base.py                  通用数据加载器基础实现。
|   |-- lru.py                   LRURec 检索器训练/评估数据加载。
|   |-- llm.py                   LLM ranker 训练和预测数据集，包括 LLMTrainDataset。
|   |-- utils.py                 Prompter、样本处理等辅助逻辑。
|   `-- templates/
|       |-- alpaca.json
|       |-- alpaca_legacy.json
|       |-- alpaca_short.json
|       |-- vigogne.json
|       `-- README.md            可选 prompt 模板定义与说明。
|
|-- model/
|   模型定义。
|   |-- lru.py                   LRURec 序列检索模型。
|   |-- llm.py                   LlamaForCausalLM/候选 verbalizer 的模型封装。
|   `-- __init__.py              模型导出。
|
|-- trainer/
|   训练、测试、日志和指标实现。
|   |-- base.py                  trainer 公共骨架。
|   |-- lru.py                   LRURec 训练、测试、候选导出。
|   |-- llm.py                   LLM ranker 训练、重排测试与 collate 关联逻辑。
|   |-- verb.py                  候选 verbalizer/输出 token 处理。
|   |-- loggers.py               W&B/本地日志适配。
|   |-- utils.py                 trainer 通用工具。
|   `-- __init__.py              trainer 导出。
|
|-- unlearning/
|   固定交互 split 的遗忘框架、方法实现和专用诊断模块。
|   |-- __init__.py
|   |-- split_builder.py
|   |   构造、验证或加载 forget/retain interaction split。
|   |-- evaluation.py
|   |   before/after prediction 收集、prediction cache、遗忘与保留指标评估。
|   |-- residual_diagnosis.py
|   |-- counterfactual_boundary_calibration.py
|   |-- collaborative_path_attribution.py
|   |-- semantic_protection.py
|   |-- structured_pruning.py
|   |   自研几何/残差/边界/协同路径/语义保护/结构化剪枝的分析组件。
|   `-- methods/
|       |-- __init__.py
|       |   METHOD_REGISTRY：将 --unlearn_method 字符串映射为策略类。
|       |-- base.py
|       |   所有 unlearning 方法的共享基类、日志与输出目录约定。
|       |-- none.py
|       |   无干预对照方法。
|       |-- random_prune.py
|       |-- selective_pruning.py
|       |-- geometry_prune.py
|       |   剪枝/几何诊断类基线或自研策略。
|       |-- retain_ft.py
|       |   基线：只以 retain_train 训练 ranker LoRA/下游参数。
|       |-- gradient_ascent.py
|       |   基线：只以 forget_train 对 ranker loss 做梯度上升。
|       |-- npo.py
|       |-- finetune.py
|       |-- llm_eraser.py
|       |-- llm_eraser_like.py
|       |-- receraser.py
|       |-- receraser_like.py
|       |   其他遗忘方法或论文风格对照实现。
|       |-- retain_prioritized_cbr_unlearning.py
|       |   保留优先的协同边界遗忘策略。
|       `-- retain_prior_cf_lora_prune.py
|           自研 retain-prior counterfactual LoRA rank pruning：Stage 1 残差定位、
|           Stage 2 retain-aware calibration、Stage 3 rank pruning；会生成 residual/
|           calibration/prune mask 等专用缓存。
|
|-- scripts/
|   |-- smoke_retain_prior_cf_lora_prune.py
|   |   小规模 smoke test，验证 retain_prior_cf_lora_prune 的关键路径。
|   `-- check_geometry_prune_outputs.py
|       核查 geometry_prune 相关实验输出。
|
|-- experiments/
|   训练 checkpoint、retrieval 候选、unlearning 运行结果、预测缓存和过程记录。
|   不应整体加入 Git；每个 run 的 config.json/run_config.json 是复现实验的首选依据。
|   |
|   |-- Llama-2-7b-hf/ml-100k/
|   |   |-- checkpoint-4400/
|   |   |   初始 LLM ranker LoRA checkpoint；unlearning 基线共同从此加载。
|   |   `-- overall_metrics.json, subset_metrics.json
|   |       ranker 基础训练的总体和子集指标。
|   |
|   |-- lru/ml-100k/
|   |   |-- retrieved.pkl           LRURec 导出的固定候选集合。
|   |   `-- test_metrics.json       retriever 测试指标。
|   |
|   |-- unlearning_sanity_seed42/
|   |   |-- split/
|   |   |   |-- forget_interactions.csv
|   |   |   |-- retain_interactions.csv
|   |   |   |-- split_metadata.json
|   |   |   |   固定交互级 forget/retain split；正式对比实验使用此组文件。
|   |   |   `-- *_neighbor_retain.csv
|   |   |       协同/语义/overlap retain 邻域辅助 split。
|   |   `-- geometry_prune/
|   |       几何剪枝 sanity run 的配置、诊断、预测、决策和指标。
|   |
|   |-- unlearning_baselines_20260727_214811/
|   |   retain_ft 与 gradient_ascent 的受控并行对比实验过程目录。
|   |   |-- README.md               实验目的、GPU 分配、控制变量。
|   |   |-- env.txt                 启动环境记录。
|   |   |-- git_status.txt          启动时的 Git 状态记录。
|   |   |-- nvidia_smi_before.txt   启动前 GPU 快照。
|   |   |-- resolved_paths.md       checkpoint/split/retrieved 等真实路径。
|   |   |-- resolved_args_*.json    两个方法的完整解析参数。
|   |   |-- commands/
|   |   |   |-- retain_ft_gpu0.sh
|   |   |   `-- gradient_ascent_gpu1.sh
|   |   |       两条正式启动命令；唯一自变量是 --unlearn_method。
|   |   |-- logs/                   两个标准输出日志与监控器日志。
|   |   |-- pids/                   GPU 任务和监控器 PID 记录。
|   |   |-- first_100_lines/        启动后首 100 行日志快照。
|   |   |-- monitor.md              周期性 GPU/日志/输出/错误监控记录。
|   |   |-- issues.md               已知风险、环境异常和过程问题。
|   |   |-- final_summary.md        两实验结束后的最终汇总位置。
|   |   `-- runs/
|   |       |-- retain_ft/
|   |       `-- gradient_ascent/
|   |           每个方法各自的 config.json、run_config.json、split_metadata.json、
|   |           predictions_before/after.json、prediction cache JSONL、method_logs.json、
|   |           metrics_unlearning.json 与 unlearning_metrics.json。
|   |
|   |-- unlearning_formal_seed42_checkpoint4400*/
|   |   retain_prior_cf_lora_prune 与 retain_ft 的正式/恢复运行。常见产物包括
|   |   residual_localization.json、retain_calibration.json、calibration_targets.json、
|   |   prune_mask*.pt、pruning_summary.json、predictions_*.json 和 metrics 文件。
|   |
|   |-- rp_cf_screen_seed42_*/
|   |   retain_prior_cf_lora_prune 的筛选、Stage 3 coarse-to-fine/full-greedy 参数实验；
|   |   保存 Stage 1/2 缓存、rank shortlist/reserve、精确打分和 pruning decisions。
|   |
|   |-- unlearning_pilot_seed42*/
|   |   geometry_prune 的 pilot、minimal、OOM-safe、step 和 smoke 变体。
|   |
|   |-- tmp_unlearning_bench_fp16/
|   |   fp16/none 对照基准。
|   `-- unlearning/
|       早期通用剪枝/语义保护产物，不对应当前正式基线过程目录。
|
|-- backups/
|   `-- stage3_prune_backup_20260713_182650/
|       Stage 3 改动前的 run_unlearning.py、unlearning/ 和 scripts/ 备份；用于代码回溯，
|       不作为当前正式入口。
|
|-- 20260713/HJY/
|   历史开发资料、思路记录、可能的局部 Git 元数据和当时的实验/备份说明。
|
|-- media/
|   |-- method.png               LlamaRec 方法图。
|   |-- performance.png          论文性能图。
|   `-- paper.pdf                相关论文副本。
|
|-- wandb/
|   offline W&B 运行记录；用于本地训练追踪。
|
|-- .agents/, .codex/
|   本地代理工具的工作目录；不是 LlamaRec 运行代码。
`-- __pycache__/
    Python 自动生成 bytecode；可安全忽略。
```

## 当前实验调用关系

```text
data/ml-100k + data/preprocessed/
        |
        v
datasets/ + dataloader/
        |
        +--> train_retriever.py --> model/lru.py + trainer/lru.py --> experiments/lru/ml-100k/retrieved.pkl
        |
        +--> train_ranker.py --> model/llm.py + trainer/llm.py --> checkpoint-4400
        |
        `--> run_unlearning.py
              |-- 固定 split: experiments/unlearning_sanity_seed42/split/
              |-- 固定候选: experiments/lru/ml-100k/retrieved.pkl
              |-- 初始权重: checkpoint-4400
              |-- 方法注册: unlearning/methods/__init__.py
              |-- 方法实现: unlearning/methods/<method>.py
              `-- 评估: unlearning/evaluation.py --> predictions_before/after + metrics
```

## 读取实验结果的优先级

1. 先读某个 run 的 `run_config.json` 与 `split_metadata.json`，确认协议与输入路径。
2. 读 `method_logs.json`，确认方法实际训练步数、学习率、参数更新量和方法状态。
3. 读 `predictions_before.json`、`predictions_after.json` 及对应 cache summary，确认预测收集状态。
4. 读 `metrics_unlearning.json` 或 `unlearning_metrics.json`，获取最终遗忘/保留/重排指标。
5. 对 retain_prior_cf_lora_prune，再读 `residual_localization.json`、`retain_calibration.json`、
   `pruning_summary.json` 和 `prune_mask*.pt`；这些文件属于自研 Stage 1/2/3，不应被误认为
   retain_ft 或 gradient_ascent 基线的产物。
